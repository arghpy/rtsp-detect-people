logLevel: warn

webrtcLocalUDPAddress: :8189
webrtcAdditionalHosts:
  - <IP>   # your host machine's LAN IP

pathDefaults:
    rtspTransport: tcp
    sourceOnDemand: no

paths:
  camera:
    source: rtsp://<USER>:<PASSWORD>@<CAMERA_IP>:$RTSP_PORT/<RTSP_FEED>
