services:
  rtsp_detect:
    image: 'ultralytics/ultralytics:latest'
    user: 1000:1000
    volumes:
      - ./:/usr/src/app
    working_dir: /usr/src/app
    command: ["python3", "rtsp_detect_people.py", "--config", "test_config.json", "--save", "--email"]
